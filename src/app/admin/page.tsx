import { AdminBereich } from "@/components/AdminBereich";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Spieltage verwalten · TC Hochheim",
};

export default function AdminSeite() {
  return <AdminBereich />;
}
